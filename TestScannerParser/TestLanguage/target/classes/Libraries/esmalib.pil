prototype int print(string s.val);
prototype int pow(int base.val, int exp.val);
int print(string s.val) {
    return 0;
}

int pow(int base.val, int exp.val) {
    int result = 1;
    for(int i = 0; i.val < exp.val; i.val = i.val + 1) {
        result.val = result.val * base.val;
    }
    return result.val;
}