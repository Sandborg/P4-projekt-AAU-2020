prototype int SwapNumbers(int a, int b);

int a = 5;
int b = 10;
SwapNumbers(a.val,b.val);

a.val = b.val + 3 + a.val;

int SwapNumbers(int a, int b) {
	int temp = a.val;
	a.val = b.val;
	b.val = temp.val;
	int lol = 9;
	lol.val = a.val;
	return 0;
}