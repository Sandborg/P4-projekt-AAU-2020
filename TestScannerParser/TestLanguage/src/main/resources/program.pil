import "esmalib.pil";
prototype int func (int a.adr);

int x = 3;
if(x.val IS 3) {
    print("awesome");
}

func(x.adr);

int func(int a.adr) {
int b = 2;
a.val = 3;
 if(a.val IS 3 AND b.val IS 2) {
           print("awesome");
  }
}